# Check if .gitignore exists
type .gitignore

# If it doesn't exist, create it:
echo functions/.env > .gitignore
echo functions/.env.* >> .gitignore
echo .env >> .gitignore
echo node_modules/ >> .gitignore

# If it exists, add these lines manually in Notepad:
notepad .gitignore
```

**Add these lines:**
```
# Sensitive files
functions/.env
functions/.env.*
.env
*.env

# Dependencies
node_modules/
functions/node_modules/

# Firebase
.firebase/
firebase-debug.log
firestore-debug.log